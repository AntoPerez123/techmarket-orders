name: Test Template v1.0.0

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Ejecutar pruebas unitarias
        run: |
          echo "Ejecutando pruebas del microservicio Orders"

      - name: Validar API
        run: |
          echo "Validando endpoints críticos"

      - name: Verificar estabilidad
        run: |
          echo "Pruebas completadas correctamente"